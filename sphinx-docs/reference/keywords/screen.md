# screen() screen\$()

`screen` returns the character code at the given screen position (row, column).

```basic
100   print screen(0,0)
```

`screen$` returns the character at the given screen position as a
single-character string.

```basic
100   print screen$(0,0)
```

# {kwd}`mid$()`

Returns a subsegment of a string, given the start position (first character is
1\) and the length, so {kwd}`mid$`{code}`(“abcdef”,3,2)` returns `“cd”`.

```basic
100 print mid$("hello",2,3)
110 print mid$("another word",2,99)
```

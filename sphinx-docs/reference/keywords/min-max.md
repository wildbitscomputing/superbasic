# {kwd}`min()` {kwd}`max()`

Returns the largest or smallest of the parameters, there can be any number of
these (at least one). You can't mix strings and integers.

```basic
100 print max(3, 42, 5)  ' prints 42
```

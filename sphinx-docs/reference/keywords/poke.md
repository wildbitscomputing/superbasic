# {kwd}`poke` {kwd}`pokew` {kwd}`pokel` {kwd}`poked`

The {kwd}`poke`, {kwd}`pokew`, {kwd}`pokel` and {kwd}`poked` functions write one
to four bytes to the specified memory address.

```basic
100 poke 4096,1: pokew $c004,$a705
```

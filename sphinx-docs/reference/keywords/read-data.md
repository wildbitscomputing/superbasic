# {kwd}`read` / {kwd}`data`

Reads from {kwd}`data` statements. The types must match. For syntactic
consistency, string data must be in quote marks.

```basic
100 read a$,b
110 data "hello world"
120 data 59
```

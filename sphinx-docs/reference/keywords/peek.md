# {kwd}`peek()` {kwd}`peekw()` {kwd}`peekl()` {kwd}`peekd()`

The {kwd}`peek`, {kwd}`peekw`, {kwd}`peekl` and {kwd}`peekd` functions retrieve
1-4 bytes from the 6502 memory space.

```basic
100 print peekd(42), peek(1)
```

# sprites

Enables and disables all sprites, optionally setting the location of the sprite
data in memory (default `$30000`). When turned on, all the sprites are erased
and their control values set to zero.

```basic
100   sprites at $18000 on
```

# verify

Compares the current BASIC program to a program stored on the current drive.
This command is a defensive measure against potential bugs in either the kernel,
the kernel drivers, or BASIC itself.

```basic
verify "game.bas"
```

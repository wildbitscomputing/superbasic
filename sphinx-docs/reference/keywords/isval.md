# {kwd}`isval()`

Returns {kwd}`true` if a string represents a valid numeric value, {kwd}`false`
otherwise. A companion to {kwd}`val()`.

```basic
100 print isval("42")
110 print isval("I like chips with salsa")
```

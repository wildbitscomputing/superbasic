# {kwd}`let`

Assignment statement. In SuperBASIC, the {kwd}`let` keyword is optional and is
provided for compatibility with classic BASIC dialects.

```basic
100 let a = 42  ' you can use LET
110 b = 17      ' ... but you don't have to
```

You can also use `@a` where `a` is a reference.

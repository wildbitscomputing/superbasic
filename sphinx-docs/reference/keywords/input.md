# {kwd}`input`

Inputs numbers or strings from the keyboard. The syntax is identical to
{kwd}`print`, but instead of displaying values, it reads input into the
specified variables. Wherever a variable appears, a value will be read from the
keyboard and stored in that variable.

```basic
100 input "Your name is ?";a$
```

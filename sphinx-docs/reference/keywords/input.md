# input

Inputs numbers or strings from the keyboard. This version uses the same syntax
as `print`, except that where there is a variable a value is entered into that
variable.

```basic
100   input "Your name is ?";a$
```

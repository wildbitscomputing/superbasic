# `:` (colon)

Statement separator. By default, only one statement is allowed per line, but you
can include multiple statements by separating them with a colon. Typically used
for semantic grouping of related statements.

```basic
100 cls: print "Hello"  ' clears the screen, then prints "Hello"
```

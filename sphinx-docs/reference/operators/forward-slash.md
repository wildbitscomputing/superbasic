# `/` (forward slash)

Floating-point division. The result is always a floating-point value, even if
both operands are integers. An error occurs if the divisor is zero.

```basic
100 print 22 / 7   ' prints 3.14285
110 print -42 / 7  ' prints -6.00000
```

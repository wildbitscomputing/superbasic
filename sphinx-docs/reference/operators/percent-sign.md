# `%` (percent sign)

Remainder operator. Returns the non-negative remainder from dividing the first
operand by the second. Both operands must be integers. The second operand must
be non-zero.

```basic
100 print 42 % 5   ' prints 2
110 print -17 % 5  ' prints 2
```

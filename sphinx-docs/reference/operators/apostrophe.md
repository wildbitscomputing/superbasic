# `'` (apostrophe)

Marks the beginning of a comment. Equivalent to {kwd}`rem`, but more concise and
does not require a colon (`:`) when used after other statements. Everything
following the apostrophe on the same line is ignored.

```basic
100 ' This is a title comment
110 print 2*2  ' prints 4
```
